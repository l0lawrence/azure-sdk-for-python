matrix:
  images:
    dockerfile1:
      image: ./Dockerfile38
      # imageBuildDir: "../.."
    dockerfile2:
      image: ./Dockerfile39
      # imageBuildDir: "../.."
    dockerfile3:
      image: ./Dockerfile310
      # imageBuildDir: "../.."
    # dockerfile4:
    #   image: ./Dockerfile311
      # imageBuildDir: "../.."
  scenarios:
    event-async:
      testTarget: event-async
    event-sync:
      testTarget: event-sync
